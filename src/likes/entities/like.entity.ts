import { Player } from 'src/players/entities/player.entity';
import { ManyToOne } from 'typeorm';

export class Like {
  // @ManyToOne((type) => Players, (player) => player.likes)
  // player: Players;
}
